// Sayfa kaydırma fonksiyonu
function scrollToSection(id) {
    document.getElementById(id).scrollIntoView({ behavior: "smooth" });
  }
  
  // İletişim formu
  function sendMessage(event) {
    event.preventDefault();
    alert("Mesajınız gönderildi.54");
  }
  